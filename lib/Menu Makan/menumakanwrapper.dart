import 'package:flutter/material.dart';
import 'menumakan.dart';

class MenuMakanWrapper extends StatelessWidget {
  const MenuMakanWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const MenuPage(); // ⬅️ TIDAK DIUBAH
  }
}
